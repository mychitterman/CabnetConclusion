Public Class RunOrganisation

    Private Sub Button1_Click(sender As Object, e As EventArgs) Handles Button1.Click
        'first check for records that have matching documents on the bcp folder.
        ' so for each record where c:/bcp files name = recordId - year  
        'we take the record and insert it into out table and then say Yes.
        'we repeat this for records where record has an A to the name with same file name then we say yes.

    End Sub
End Class